interface Player {
    id: number
    firstName: string
    lastName: string
    birthDay: Date
    height: number
    weight: number
    isActive: boolean
    createdAt: Date
    updatedAt: Date
}

export type {Player}
