interface Player {
    id: number
    firstName: string
    lastName: string
    birthDate: Date
    height: number
    weight: number
    isActive: boolean
    createdAt: Date
    updatedAt: Date
}

export type {Player}
