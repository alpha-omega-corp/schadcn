interface Player {
    id: number
    avatar: string
    firstName: string
    lastName: string
    number: number
    birthDate: Date
    height: number
    weight: number
    isActive: boolean
    createdAt: Date
    updatedAt: Date
}

export type {Player}
